# Claude Widget

## Git Rules

- **Never add co-author lines** to commits. No `Co-Authored-By` trailers ever.
- Use `rahul2123` GitHub account for this repo.

## Project

macOS menu bar app showing Claude API usage stats. Built with Swift + AppKit (NSPanel). No Xcode/WidgetKit — builds via `build.sh`.
